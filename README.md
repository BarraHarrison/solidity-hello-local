# Solidity Hello Local

A simple Solidity project to deploy a contract locally using Node.js, ethers.js, solc, and Ganache — without Hardhat or MetaMask.

## Setup

1. Install Node.js dependencies:

```bash
npm install
